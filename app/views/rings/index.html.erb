<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
    <style>
    body{
      width:1000px;
      position: relative;
      margin: auto;
    }
    .ring{
      display:inline-block;
      width:263px;
      height:263px;
      background-size: contain;
    }
    </style>
  </head>
  <body>
    <h1>All Products</h1>
    <% @rings.each do |ring| %>
    <%=link_to image_tag(ring.img_url, class:"ring"), ring_path(ring)%>
    <!-- <%= link_to ring.shape, ring_path(ring) %> -->
    <%end%>

  </body>
</html>
