<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
    <style>
    body{
      background-image: url("https://www.ritani.com/wp-content/uploads/2015/02/ritani-engagement-rings.jpg");
      background-size: cover;
      background-repeat: no-repeat;
    }
    .button{
      display: inline-block;
      font-weight: bold;
      color: white;
      text-align: center;
      font-size: 20px;
      text-decoration: none;
      padding: 10px;
    }
    </style>
  </head>
  <body>
    <% if @current_user %>
    <div>
      <%= link_to "Welcome, #{@current_user.name}!", retailer_path(@current_user) %>
      <%= link_to "Sign Out", session_path, method: :delete %>
    <% else %>
    <div class="button">
      <%= link_to "Sign Up", new_retailer_path %>
    </div>
    <div class = "button">
      <%= link_to "Sign In", new_session_path %>
    </div>
    <div class = "button">
      <%= link_to "Shop by Brands", retailers_path %>
    </div>
    <div class = "button">
      <%= link_to "Shop All Products", rings_path %>
    </div>


    <% end %>


    <!-- <% if @current_user %>
  <%= link_to "Welcome, #{@current_user.name}!", retailer_path(@current_user) %>
  <%= link_to "Sign Out", session_path, method: :delete %>
<% else %>
  <%= link_to "Sign In", new_session_path %>
<% end %> -->


  </body>
</html>
