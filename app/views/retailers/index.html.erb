<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>

  <style>
  body{
    width: 1000px;
    position: relative;
    text-align: center;
    margin:auto;
  }
  nav{
    background-color:grey;
  }
  ul{
    height: 40px;
    list-style-type: none;
    margin: 0;
    padding: 0;
    overflow: hidden;
  }

  li{
    float: right;
  }
  .image{

    margin:40px;

    height: 280px;
    width: 280px;
  }
  img{
    width:1000px;
  }

  nav li{
    display:block;
    font-weight: 700;
    text-align: center;
    font-size: 20px;
    padding-left: 10px;
    padding-right: 10px;
    margin:auto;


  }

  </style>
  </head>
  <body>
    <div>
      <img src="http://www.moissanitebridal.com/store/graphics/00000002/engagement-ring-banner.jpg">
    </div>
    <nav>
      <ul>
        <li><%=link_to 'Shop All Products'.upcase, rings_path%></li>
       </ul>
    </nav>
    <div class="retailers">
    <% @retailers.each do |retailer| %>
    <!-- <%= link_to retailer.name, retailer_path(retailer)%> -->
    <%= link_to image_tag(retailer.img_url, class:"image"), retailer_path(retailer)%>
    <!-- <img src="<%= retailer.img_url %>" class="image"> -->
    <%if @current_user == retailer %>
     <%= link_to "Edit", edit_retailer_path(retailer) if @current_user %>
     <%= link_to 'Delete', retailer_path(retailer), method: :delete, data:{confirm:'Are You Sure?'} if @current_user %>
     <%end%>
    <%end%>
  </div>
  </body>
</html>
